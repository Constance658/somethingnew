<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>S</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@500&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      background: linear-gradient(#dceeff, #a0d8ef);
      overflow: hidden;
      font-family: 'Comfortaa', cursive;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      text-align: center;
    }

    h1 {
      color: #2b4f81;
      font-size: 2em;
      max-width: 90%;
      line-height: 1.6;
      padding: 20px;
    }

    .cloud {
      position: absolute;
      background: white;
      border-radius: 50%;
      opacity: 0.7;
      animation: float 60s linear infinite;
    }

    .cloud::before,
    .cloud::after {
      content: '';
      position: absolute;
      background: white;
      border-radius: 50%;
    }

    .cloud1 {
      width: 200px;
      height: 60px;
      top: 40px;
      left: -250px;
      animation-delay: 0s;
    }

    .cloud1::before {
      width: 100px;
      height: 100px;
      top: -30px;
      left: 50px;
    }

    .cloud1::after {
      width: 120px;
      height: 120px;
      top: -40px;
      left: 100px;
    }

    .cloud2 {
      width: 150px;
      height: 50px;
      top: 150px;
      left: -200px;
      animation-delay: 15s;
    }

    .cloud2::before {
      width: 80px;
      height: 80px;
      top: -20px;
      left: 40px;
    }

    .cloud2::after {
      width: 100px;
      height: 100px;
      top: -30px;
      left: 80px;
    }

    @keyframes float {
      0% {
        transform: translateX(0);
      }
      100% {
        transform: translateX(120vw);
      }
    }

    @media (max-width: 480px) {
      h1 {
        font-size: 1.5em;
        padding: 15px;
      }
    }
  </style>
</head>
<body>
  <div class="cloud cloud1"></div>
  <div class="cloud cloud2"></div>
  <h1>Люблю Сердара :)<br>П.с. когда зайдёшь, просто улыбнись 💙</h1>
</body>
</html>
